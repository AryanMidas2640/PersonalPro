# Server Configuration
#server.port=8080
#server.servlet.context-path=/

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://aryanofficialdev_db_user:4weS3hXx8PaempgJ@cluster0.ljklyti.mongodb.net/addressDBretryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.database=addressDB
# Security
#spring.security.user.name=admin
#spring.security.user.password=admin

# JWT Configuration
jwt.secret=${JWT_SECRET:mysecretkeymysecretkeymysecretkey123}
jwt.expiration=86400000

spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# Swagger
#springdoc.api-docs.path=/api-docs
#springdoc.swagger-ui.path=/swagger-ui
# Application Settings
spring.application.name=AddressAPI

# Logical default DB key
db.defaultDb=primary
server.port=9090

# Logical name ? Actual Mongo DB name mapping

db.databases.primary=addressDB
server.servlet.context-path=/
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
# remove springdoc.swagger-ui.path, default index.html use hoga